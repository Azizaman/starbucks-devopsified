apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "starbucks-helm.fullname" . }}-test-connection"
  labels:
    {{- include "starbucks-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "starbucks-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
